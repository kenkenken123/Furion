# Web 应用开发

## 5.1 动态 WebAPI
Furion 框架的核心功能之一是动态 WebAPI，它可以极大地简化 API 接口的创建过程。开发者无需手动编写 Controller 类，框架会自动将符合特定规范的普通类方法转换为 WebAPI 接口。

主要特点：
- **约定即路由**：通过类名、方法名、以及方法参数等约定，自动生成 API 路由和 HTTP 方法。
- **特性增强**：可以使用特性（Attributes）来进一步定制路由、HTTP 方法、参数绑定、版本控制等。
- **无需继承**：服务类不需要继承特定的基类（如 `ControllerBase`）。
- **依赖注入友好**：动态 WebAPI 与 Furion 的依赖注入系统无缝集成。

## 5.2 HttpContext
在 Web 应用开发中，`HttpContext` 对象封装了所有关于单个 HTTP 请求和响应的信息。Furion 提供了便捷的方式来访问和操作 `HttpContext`。

主要用法：
- **静态访问**：通过 `App.HttpContext` 可以方便地在应用的任何地方（需要注意作用域）获取当前的 `HttpContext`。
- **依赖注入**：可以通过依赖注入 `IHttpContextAccessor` 来安全地访问 `HttpContext`。
- **常用信息获取**：轻松获取请求路径、查询参数、请求头、表单数据、用户信息、IP 地址等。
- **响应操作**：设置响应状态码、响应头、写入响应体等。

## 5.3 筛选器/拦截器/过滤器/AOP
ASP.NET Core 中的筛选器（Filters）允许在请求处理管道的特定阶段执行代码，例如在控制器操作执行之前或之后。Furion 在此基础上提供了更便捷的 AOP (面向切面编程) 实现。

主要类型和用途：
- **授权筛选器 (Authorization Filters)**：用于权限验证。
- **资源筛选器 (Resource Filters)**：在模型绑定之前和之后执行，可用于缓存或短路请求。
- **操作筛选器 (Action Filters)**：在操作方法执行之前和之后执行，可用于修改参数或结果。
- **异常筛选器 (Exception Filters)**：全局处理未经捕获的异常。
- **结果筛选器 (Result Filters)**：在操作结果执行之前和之后执行，可用于修改响应。
- **Furion AOP**：提供了更细粒度的拦截能力，如方法调用拦截、属性拦截等，通常通过特性来实现。

## 5.4 请求审计日志 (Audit)
记录应用程序的详细操作日志对于系统监控、问题排查和安全审计至关重要。Furion 提供了请求审计日志功能。

主要特点：
- **自动记录**：可以自动记录请求的详细信息，如请求者、请求时间、IP 地址、浏览器信息、请求参数、执行耗时、响应结果等。
- **可配置性**：可以配置审计日志的记录级别、存储方式（如数据库、文件）。
- **自定义扩展**：允许开发者自定义审计信息的收集和存储逻辑。
- **敏感信息处理**：通常会提供机制来避免记录密码等敏感信息。

## 5.5 中间件 (Middleware)
中间件是构成 ASP.NET Core 请求处理管道的基本单元。Furion 框架本身就构建在众多中间件之上，并且允许开发者方便地注册和使用自定义中间件。

主要用途：
- **请求处理**：在请求到达控制器或终端节点之前，或在响应返回客户端之前，对 HTTP 请求和响应进行处理。
- **常见功能**：如身份验证、授权、日志记录、异常处理、静态文件服务、CORS（跨域资源共享）等。
- **Furion 中的使用**：`app.UseInject()` 方法就会注册一系列 Furion 核心中间件。开发者也可以通过 `app.Use()` 或 `app.UseMiddleware<TMiddleware>()` 来添加自定义中间件。

## 5.6 Vue/React/Angular 请求代理
在前后端分离的架构中，前端应用（如 Vue, React, Angular）通常需要调用后端 API。Furion 提供了简化这种交互的机制，可能包括：

- **API 客户端生成**：可能提供工具或约定来根据后端 API 定义自动生成前端可用的服务代理代码（如 TypeScript 或 JavaScript）。
- **规范化接口适配**：确保 Furion 的规范化接口（如统一响应格式）能被前端框架轻松消费。
- **开发时代理**：简化开发过程中的跨域请求问题，例如通过内置的代理服务器。

（注意：此部分的具体实现细节需参考 Furion 文档中关于前端集成的具体说明。）
