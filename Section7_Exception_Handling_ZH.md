# 友好异常处理

## 7.1 什么是异常及处理方式
异常是在程序执行期间发生的非正常事件，它会中断程序的正常流程。常见的处理方式包括 `try-catch` 块、全局异常过滤器等。

## 7.3 什么是友好异常处理
友好异常处理旨在向用户或API调用者提供清晰、易于理解且有用的错误信息，而不是原始的、可能包含敏感信息的系统异常堆栈。Furion框架通过 `Oops.Oh` 静态类和相关机制实现这一目标。

主要特点：
- **用户友好**：向最终用户展示易懂的错误提示。
- **开发友好**：为开发者提供清晰的错误定位和调试信息。
- **避免敏感信息泄露**：防止将原始堆栈跟踪等信息暴露给客户端。

## 7.4 友好异常处理使用示例
在 Furion 中使用友好异常处理非常简单：
1.  **注册服务**：通常通过 `services.AddFriendlyException();` 在 `Program.cs` (或 `Startup.cs` 的 `ConfigureServices`) 中注册。
2.  **抛出异常**：使用 `throw Oops.Oh("错误消息");` 或 `Oops.Oh("错误消息").Throw();` 来抛出业务逻辑异常。可以指定错误码，如 `Oops.Oh(1001, "错误消息");`。

## 7.5 关于 Oops.Oh
`Oops.Oh` 是 Furion 框架中用于抛出友好异常的核心静态类。
- **命名含义**：“Oops” 模仿人们在犯错时的口头禅，“Oh” 表示一种恍然大悟或无奈的情感。
- **重载方法**：提供了多种重载，允许只传入错误消息、错误码和消息、或格式化消息等。

## 7.6 最佳实践
- **创建异常信息类型**：建议使用枚举或静态类来统一定义错误码和对应的错误消息模板，方便管理和多语言支持。通过 `[ErrorCodeItemMetadata]` 特性可以为枚举项或类成员附加元数据（如错误消息模板）。
- **静态异常类使用**：将定义好的错误码和消息封装在静态类中，通过静态方法抛出，如 `throw MyErrorCode.UserNotFound.Oh();`。
- **异常方法重试**：Furion 提供了 `Retry.Invoke()` 和 `Retry.InvokeAsync()` 方法，可以方便地对可能抛出异常的方法进行重试。

## 7.10 [IfException] 特性使用
`[IfException]` 特性可以标记在方法上，当方法执行出现特定类型的异常时，自动替换为预设的友好异常信息。

## 7.11 异常消息优先级
Furion 处理异常消息时，会按照以下优先级确定最终显示的错误信息：
1.  `Oops.Oh("覆盖消息").StatusCode(xxx);` 中的覆盖消息。
2.  `[IfException]` 特性中指定的错误消息。
3.  通过 `[ErrorCodeItemMetadata]` 或 `appsettings.json` 配置的错误码对应的消息。
4.  直接在 `Oops.Oh(错误码)` 中指定的默认消息（如果适用）。
5.  异常对象的 `Message` 属性。

## 7.12 多语言支持
友好异常处理支持多语言，可以根据当前请求的区域性设置返回对应语言的错误消息。需要在本地化配置中提供不同语言的错误消息文本。

## 7.13 规范化结果异常处理
当启用规范化结果时，友好异常（如通过 `Oops.Oh` 抛出的）会被自动转换为统一的JSON响应格式，其中包含错误码和错误消息，而不是将原始异常信息直接暴露给客户端。

## 7.14 全局异常处理提供器
Furion 允许注册自定义的全局异常处理提供器 (`IGlobalExceptionHandler`)，以便开发者可以完全接管异常处理逻辑，例如记录到特定的日志系统或执行自定义的响应。

## 7.15 FriendlyExceptionSettings 配置
可以通过 `appsettings.json` 中的 `FriendlyExceptionSettings`节点或代码配置友好异常处理的行为，如是否隐藏原始异常信息、默认错误码等。

## 7.16 BadPageResult 错误页
对于非API请求（如MVC视图请求）发生的异常，Furion 支持通过 `BadPageResult` 返回一个友好的错误页面，而不是显示原始的错误堆栈。
